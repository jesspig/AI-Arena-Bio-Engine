{
  "name": "bio-engine",
  "version": "1.0.0",
  "description": "Bio-Engine - 生物引擎 AI 模型能力对比测试主题项目",
  "type": "module",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "build:portal": "cd portal && bun install && bun run build",
    "build:kimi-k2.6": "cd kimi-k2.6 && bun install && bun run build",
    "build:glm-5.1": "cd glm-5.1 && bun install && bun run build",
    "build:deepseek-v4": "cd deepseek-v4 && bun install && bun run build",
    "build:mimo-v2.5-pro": "cd mimo-v2.5-pro && bun install && bun run build",
    "build": "bun run build:portal && bun run build:kimi-k2.6 && bun run build:glm-5.1 && bun run build:deepseek-v4 && bun run build:mimo-v2.5-pro",
    "build:turbo": "turbo run build",
    "dev": "cd portal && bun run dev",
    "dev:wrangler": "wrangler dev --local",
    "clean": "rm -rf portal/public",
    "deploy": "bun run build && wrangler deploy",
    "deploy:turbo": "turbo run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250520.0",
    "turbo": "^2.0.0",
    "wrangler": "^4.12.0"
  },
  "workspaces": [
    "portal",
    "kimi-k2.6",
    "glm-5.1",
    "deepseek-v4",
    "mimo-v2.5-pro"
  ]
}
